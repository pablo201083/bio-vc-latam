# Include Quality Completion Plan

## Status

- `163` startups in `include`
- `163` already `confirmed`
- `0` still missing minimum external-source completion
- `105` are `reviewed`
- `58` remain `seeded`

## Completion Definition

The minimum quality bar for the `include` universe is now met:

- `source_url` from an auditable external source
- `source_type`
- `evidence_excerpt`
- `startup_summary_v1`
- `scope_status = confirmed`
- `review_status = seeded` or `reviewed`

## What Was Closed

The last critical `taxonomy_stub + provisional` cases were resolved and the only clearly off-thesis case in the remnant (`AbAstra`) was removed from `include`.

Closed in the final pass:

- `Huiro`
- `Branch Energy`
- `Radbio`
- `Syocin Biotech`
- `Ocular Bio Design`
- `PreGen`
- `AbAstra` -> `exclude`

## Next Quality Frontier

The remaining work is no longer about missing external source coverage. It is now about hardening the `seeded` rows into stronger `reviewed` profiles.

Priority order:

1. `seeded` startups in high-signal thesis themes
2. edge cases in `climate, energy and resource systems`
3. seed rows using portfolio/public-profile sources that should be upgraded to official websites where available

## Recommended Next Sprint

Convert the remaining `58` `seeded` includes into `reviewed`, starting with:

- `food biotech and novel ingredients`
- `biomanufacturing and bioindustrial platforms`
- `diagnostics and medtech`
- `climate, energy and resource systems`
